{
  "name": "news-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "pm2 start server.js --watch --ignore-watch 'node_modules'",
    "status": "pm2 status",
    "stop": "pm2 stop server",
    "restart": "pm2 restart server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "request": "^2.88.0"
  }
}
